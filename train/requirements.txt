# ---------------------------------------------------------------------------
# Python dependencies.
#
# TIERS -- most of this project runs WITHOUT the heavy ones:
#
#   Core (needed for bit-exactness, export and analysis):
#       numpy
#   Training only (needed to produce a new model, not to use one):
#       torch, scikit-learn, librosa, soundfile, scipy
#   Analysis and plotting:
#       pandas, statsmodels, matplotlib
#   Measurement (needs the physical PPK2 hardware):
#       pyserial, ppk2-api
#
# train/verify_parity.py -- the harness everything depends on -- needs ONLY
# numpy, deliberately. See the module docstring in train/quant_ref.py.
#
# Install:
#     python3 -m venv .venv && source .venv/bin/activate
#     pip install -r train/requirements.txt
# ---------------------------------------------------------------------------

# --- core -------------------------------------------------------------------
numpy>=1.24

# --- config -----------------------------------------------------------------
# train/config.py falls back to a tiny built-in parser if this is absent, so
# the pipeline still runs without it.
PyYAML>=6.0

# --- training ---------------------------------------------------------------
torch>=2.0
scikit-learn>=1.3
scipy>=1.10
librosa>=0.10
soundfile>=0.12

# --- analysis ---------------------------------------------------------------
pandas>=2.0
statsmodels>=0.14
matplotlib>=3.7

# --- measurement (physical hardware) ---------------------------------------
pyserial>=3.5
# ppk2-api provides the Nordic Power Profiler Kit II interface.
ppk2-api>=0.9.2
